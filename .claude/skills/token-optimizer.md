---
name: token-optimizer
description: Reduces token usage in long sessions
---

# Token Optimizer Skill

- Compress repeated boilerplate with "same pattern as [filename]"
- Use file path headers instead of reprinting full context
- Summarize completed phases in 3 bullets before moving on
- Prefer diffing existing files over reprinting entire files
- Reference schemas/interfaces by name instead of reprinting
- Skip trivial getter/setter implementations — note pattern once
